# Lightweight requirements for free-tier CPU deployment
# (excludes training-only packages to reduce image size and install time)

torch>=2.0.0
transformers>=4.36.0
accelerate>=0.25.0
gradio>=4.0.0
bcrypt>=4.0.0
pymongo>=4.6.0
huggingface_hub>=0.19.0
