version: "3.8"

services:
  medchat:
    build: .
    ports:
      - "7860:7860"
    volumes:
      - medchat-data:/app/data
    environment:
      - GRADIO_SERVER_NAME=0.0.0.0
    restart: unless-stopped

volumes:
  medchat-data:
